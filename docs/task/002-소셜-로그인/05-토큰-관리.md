# 05. 토큰 관리 (SecureStore)

## 목표

`expo-secure-store`를 사용하여 JWT 토큰을 안전하게 저장/조회/삭제합니다.

## 상태

✅ 완료

## 선행 조건

- [x] 03-apple-로그인-구현 완료
- [x] 04-kakao-로그인-구현 완료
- [x] 04.5-merge-안정화 완료

## 현재 프로젝트 상태 (Merge 후)

### 이미 완료된 항목

- [x] `expo-secure-store` 설치됨 (package.json)
- [x] `src/lib/auth/auth.ts` 추가됨 (백엔드 API 함수)

### 현재 파일 구조

```
src/lib/
├── auth/
│   ├── index.ts          # SDK export (Apple, Kakao)
│   ├── AppleAuth.ts      # Apple SDK
│   ├── KakaoAuth.ts      # Kakao SDK
│   └── auth.ts           # 백엔드 API 함수 (merge됨) - @/types/auth import 에러
├── use-color-scheme.ts
├── use-color-scheme.web.ts
└── use-theme-color.ts
```

### 해결해야 할 문제

`auth.ts`에서 `@/types/auth` import 에러 발생:
```typescript
import { AuthToken, KakaoAuthToken, SignupFormData, KakaoSignupFormData } from '@/types/auth';
```

**해결 방안**: 타입을 `auth.ts` 파일 내에 직접 정의하고 export

## 왜 SecureStore인가?

| 저장소 | 보안 수준 | 사용 |
|--------|----------|------|
| AsyncStorage | 낮음 (평문) | 비민감 데이터 |
| **SecureStore** | 높음 (암호화) | **토큰, 민감 정보** |
| MMKV | 중간 | 대용량 데이터 |

SecureStore는 내부적으로:
- **iOS**: Keychain 사용 (Secure Enclave 보호)
- **Android**: Keystore 사용 (TEE 보호)

## 아키텍처: useAuth 폴더 구조

토큰 저장소를 `useAuth` hook 내부로 캡슐화하여 외부에서 직접 접근을 방지합니다.

**장점:**
- `tokenStorage`와 `tokenUtils`는 외부에서 직접 import 불가
- 오직 `useAuth` hook을 통해서만 토큰 관리 가능
- 관련 파일들이 한 폴더에 모여 있어 응집도 높음

## 작업 절차

### 1. auth.ts 타입 내부 정의로 수정

`@/types/auth` import를 제거하고 파일 내에 직접 타입 정의:

#### src/lib/auth/auth.ts

```typescript
import { API_URL } from '@/constants';

export interface AuthToken {
  accessToken: string;
  refreshToken: string;
}

export interface KakaoAuthToken {
  registrationToken?: string;
  accessToken?: string;
  refreshToken?: string;
}

export interface SignupFormData {
  email: string;
  password: string;
  nickname: string;
  privacyPolicy: boolean;
  termsOfService: boolean;
}

export interface KakaoSignupFormData {
  nickname: string;
  privacyPolicy: boolean;
  termsOfService: boolean;
}

export async function loginWithEmail(email: string, password: string): Promise<AuthToken> {
  // ... 기존 코드 유지
}

// ... 나머지 함수들
```

### 2. useAuth 폴더 생성 및 토큰 저장소 구현

#### src/lib/auth/useAuth/tokenStorage.ts (내부 전용)

```typescript
import * as SecureStore from 'expo-secure-store';

enum StorageKey {
  AccessToken = 'accessToken',
  RefreshToken = 'refreshToken',
}

export interface Tokens {
  accessToken: string;
  refreshToken: string;
}

const saveTokens = async (tokens: Tokens): Promise<void> => {
  await Promise.all([
    SecureStore.setItemAsync(StorageKey.AccessToken, tokens.accessToken),
    SecureStore.setItemAsync(StorageKey.RefreshToken, tokens.refreshToken),
  ]);
};

const getAccessToken = async (): Promise<string | null> => {
  return SecureStore.getItemAsync(StorageKey.AccessToken);
};

const getRefreshToken = async (): Promise<string | null> => {
  return SecureStore.getItemAsync(StorageKey.RefreshToken);
};

const getTokens = async (): Promise<Tokens | null> => {
  const [accessToken, refreshToken] = await Promise.all([
    getAccessToken(),
    getRefreshToken(),
  ]);

  if (!accessToken || !refreshToken) {
    return null;
  }

  return { accessToken, refreshToken };
};

const clearTokens = async (): Promise<void> => {
  await Promise.all([
    SecureStore.deleteItemAsync(StorageKey.AccessToken),
    SecureStore.deleteItemAsync(StorageKey.RefreshToken),
  ]);
};

export const tokenStorage = {
  saveTokens,
  getTokens,
  getAccessToken,
  getRefreshToken,
  clearTokens,
};
```

### 3. 토큰 유효성 검사 유틸리티

#### src/lib/auth/useAuth/tokenUtils.ts (내부 전용)

```typescript
interface JWTPayload {
  exp?: number;
  iat?: number;
  sub?: string;
  [key: string]: unknown;
}

const decodeJWT = (token: string): JWTPayload | null => {
  try {
    const base64Url = token.split('.')[1];
    const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');
    const jsonPayload = decodeURIComponent(
      atob(base64)
        .split('')
        .map((c) => '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2))
        .join('')
    );
    return JSON.parse(jsonPayload);
  } catch {
    return null;
  }
};

const isTokenExpired = (token: string): boolean => {
  const payload = decodeJWT(token);
  if (!payload || typeof payload.exp !== 'number') {
    return true;
  }

  const now = Math.floor(Date.now() / 1000);
  return payload.exp < now + 10;
};

const getTokenRemainingTime = (token: string): number => {
  const payload = decodeJWT(token);
  if (!payload || typeof payload.exp !== 'number') {
    return 0;
  }

  const now = Math.floor(Date.now() / 1000);
  return Math.max(0, payload.exp - now);
};

export const tokenUtils = {
  decodeJWT,
  isTokenExpired,
  getTokenRemainingTime,
};
```

### 4. 인증 상태 관리 Hook

#### src/lib/auth/useAuth/useAuth.ts

```typescript
import { useState, useEffect, useCallback } from 'react';
import { tokenStorage, type Tokens, type UserInfo } from './tokenStorage';
import { tokenUtils } from './tokenUtils';

export interface AuthState {
  isLoading: boolean;
  isAuthenticated: boolean;
  tokens: Tokens | null;
}

export interface AuthActions {
  login: (tokens: Tokens) => Promise<void>;
  logout: () => Promise<void>;
  refreshAuth: () => Promise<void>;
}
```

### 5. useAuth 폴더 index.ts (Public API)

#### src/lib/auth/useAuth/index.ts

```typescript
export { useAuth, AuthProvider } from './AuthContext';
export type { AuthState, AuthActions } from './AuthContext';
export type { Tokens } from './tokenStorage';
```

**중요:**
- `tokenStorage`와 `tokenUtils`는 export하지 않음 (내부 전용)
- `useAuth`는 Context 기반으로 구현됨 (AuthProvider 필요)
- 자세한 구현은 [07.1-로그인-버그-수정-및-UX-개선.md](./07.1-로그인-버그-수정-및-UX-개선.md) 참조

### 6. auth/index.ts export 업데이트

#### src/lib/auth/index.ts

```typescript
export { isAppleLoginAvailable, signInWithApple } from './AppleAuth';
export { signInWithKakao, signOutFromKakao } from './KakaoAuth';

export {
  loginWithEmail,
  signUp,
  kakaoSignUp,
  refreshAccessToken,
  kakaoLogin,
} from './auth';

export type { AppleLoginResult } from './AppleAuth';
export type { KakaoLoginResult } from './KakaoAuth';
export type {
  AuthToken,
  KakaoAuthToken,
  SignupFormData,
  KakaoSignupFormData,
} from './auth';
```

## 최종 파일 구조

```
src/lib/
├── auth/
│   ├── index.ts          # 통합 export (SDK + API + 타입)
│   ├── AppleAuth.ts      # Apple SDK + AppleLoginResult 타입
│   ├── KakaoAuth.ts      # Kakao SDK + KakaoLoginResult 타입
│   ├── auth.ts           # 백엔드 API + AuthToken 등 타입
│   └── useAuth/
│       ├── index.ts          # Public API (useAuth hook + 타입만 export)
│       ├── useAuth.ts        # 인증 상태 Hook
│       ├── tokenStorage.ts   # 내부 전용 (index에서 export 안함)
│       └── tokenUtils.ts     # 내부 전용 (index에서 export 안함)
├── use-color-scheme.ts
├── use-color-scheme.web.ts
└── use-theme-color.ts
```

**접근 제어:**
- 외부에서 접근 가능: `useAuth`, `AuthProvider`, `AuthState`, `AuthActions`, `Tokens`
- 외부에서 접근 불가: `tokenStorage` 함수들, `tokenUtils` 함수들

## 완료 조건

- [x] expo-secure-store 설치 (이미 완료)
- [x] `src/lib/auth/auth.ts` 타입 내부 정의로 수정
- [x] `src/lib/auth/index.ts` export 업데이트
- [x] `src/lib/auth/useAuth/` 폴더 생성
- [x] `src/lib/auth/useAuth/tokenStorage.ts` 구현
- [x] `src/lib/auth/useAuth/tokenUtils.ts` 구현
- [x] `src/lib/auth/useAuth/useAuth.ts` Hook 구현
- [x] `src/lib/auth/useAuth/index.ts` Public API export
- [x] TypeScript 에러 없음 확인

## 참고 자료

- [expo-secure-store](https://docs.expo.dev/versions/latest/sdk/securestore/)
- [iOS Keychain Services](https://developer.apple.com/documentation/security/keychain_services)
